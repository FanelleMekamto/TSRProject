<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resource="clr-namespace:TSR.Resources"
             x:Class="TSR.MainPage">

    <ContentPage.Content>
        <StackLayout Padding="20" VerticalOptions="CenterAndExpand">
            <Label  Text="{x:Static resource:AppResources.SelectLanguage}"  />
            <Picker x:Name="pickerLanguage">
                <Picker.Items>
                    <x:String>English</x:String>
                    <x:String>French</x:String>
                </Picker.Items>
            </Picker>

            <Button Text="{x:Static resource:AppResources.Next}"
                       Clicked="OnUpdateLangugeClicked"
                       BackgroundColor="Black"
                       TextColor="White"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
